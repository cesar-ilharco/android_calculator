package com.myapp.calculator.buttons;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.Button;

/**
 * Android calculator app
 */
public class NumericButton extends Button {
    public NumericButton(Context context) {
        super(context);
    }

    public NumericButton(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public NumericButton(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public NumericButton(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
